{
  "navigationFallback": {
    "rewrite": "/index.html",
    "exclude": ["/api/*", "/*.json", "/*.png", "/*.ico", "/*.svg"]
  },
  "routes": [
    { "route": "/api/*", "methods": ["GET", "POST", "PUT", "OPTIONS"] }
  ],
  "responseOverrides": {
    "404": { "rewrite": "/index.html" }
  }
}
